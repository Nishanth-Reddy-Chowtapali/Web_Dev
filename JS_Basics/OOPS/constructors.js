function Person(name, age) {
  this.name = name;
  this.age = name;
}

function Car(make, model) {
  this.make = make;
  this.model = model;
}

let myCar = new Car("Toyota", "Camry");
console.log(myCar);
